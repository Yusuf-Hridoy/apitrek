# Required: primary AI provider
MISTRAL_API_KEY=
# MISTRAL_MODEL=mistral-large-latest

# Optional: fallback AI providers (used automatically if the primary fails)
GROQ_API_KEY=
GITHUB_MODELS_API_KEY=

# Optional: override fallback models
GROQ_MODEL=llama-3.3-70b-versatile
GITHUB_MODEL=gpt-4o

# Optional: SQLite location for session history
DATABASE_PATH=data/api_sentinel.db
